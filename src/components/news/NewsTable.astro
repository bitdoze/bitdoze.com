---
// Overrides the table element for news digests: wraps the table in a
// horizontal scroll container per DESIGN.md ("Tables: scroll horizontally").
// Cell styling lives in NewsLayout's .digest-body styles.
---

<div class="digest-table-wrap">
  <table>
    <slot />
  </table>
</div>
